using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UGUITest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

// UGUI -> 界面
//UGUI ： unity 内置 的一个框架
//UGUI 每一个节点 RectTransform 都继承于 tranform； RectTransform 不允许 修改
//UGUI 每一个节点 都是 继承自 UIView
//UGUI 每一个节点 都是 继承自 UIControl
//UGUI 每一个节点 都是 继承自 UIRender
